#include <stdio.h>
#include <math.h>

int main()
{
    float a, b;
     printf("Enter a -> ");
    scanf("%f", &a);
    printf("Enter b -> ");
    scanf("%f", &b);
    s = a+a 
    if (!a && !b)
        return 0;

    return 0;
}
